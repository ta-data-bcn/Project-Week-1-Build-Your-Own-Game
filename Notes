
1 - Welcome message

2 - explain user how it works

3 - ask user to input message to encrypt

4 - tell user that now his message is safe

5 - ask user to decrypt message  


CODE 

message = input("Please enter the message yout would like to encrypt")

encryption_key = "sadfsgdgdasrewrafasfdgsdgasfsansaknsadnsadndfsbnsd"

encrypt_message = ""

for i in message: 
	position = encryption_key.find(i)
	newposition = position + 3
	encrypt_message+=encryption_key[newposition]
print(encrypt_message)



CODE


#def user_message():

def encryption(): 
    message = input("Please enter the message yout would like to encrypt: ")
    global encryption_key
    encryption_key = "ab&cdefg$hijkl@mnopqr&set¿uv?wxyz"
    global encrypt_message
    encrypt_message = ""
    for i in message: 
        input_value = encryption_key.find(i)
        new_value = (input_value + 5) % 32
        encrypt_message+=encryption_key[new_value]
    print(encrypt_message)

def activate_decript():
    decript_message = ()
    
def decryption():
    decrypt_message = ""
    for i in encrypt_message: 
        recover_value = encryption_key.find(i)
        old_position = (recover_value - 5) % 32
        decrypt_message+=encryption_key[old_position]
    print(decrypt_message)
